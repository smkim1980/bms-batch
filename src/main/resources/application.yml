spring:
  config:
    import:
        - classpath:yaml/local/application-local.yml
        - classpath:yaml/dev/application-dev.yml
        - classpath:yaml/prod/application-prod.yml
  application:
    name: bms-batch
  profiles:
    active: local
  batch:
    job:
      enabled: false
      # name: ${job.name:}